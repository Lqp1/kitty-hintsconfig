---
links:
  - regexp: '(https?://[^\s]*)'
    link: '{}'
  - regexp: 'JIRA: ([A-Z]+-[0-9]+)'
    link: 'https://jira.mycompany.com/{}'
markers:
  - regexp: (error|warning)
    marker: 3
  - regexp: info
    marker: 1
